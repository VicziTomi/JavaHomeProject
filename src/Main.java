import java.util.Arrays;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {


        GUI gui = new GUI();

        /*
        // final GameArea gameArea = new GameArea(4);


        // GameFrame gameFrame = new GameFrame();
        PiecePool p = new PiecePool();
        // System.out.println(p);
        // p.remove(16);
        // p.remove(12);
        // System.out.println(p);

        GameArea gameArea = new GameArea();


        gameArea.putPiece(p.swsy, 0, 1);
        System.out.println("-------------------");
        gameArea.putPiece(p.tbsn, 0, 2);

        System.out.println("-------------------");
        gameArea.putPiece(p.swsy, 0, 0);


        System.out.println(p);

        Scanner scn = new Scanner(System.in);
        String line = scn.nextLine();
        Piece[] temp = new Piece[1];
        while (!line.equals("quit")) {

            String[] choose = line.split(" ");
            if (choose.length == 1) {
                int z = Integer.parseInt(choose[0]);
                // temp[0] = p.getPiece(z);
                System.out.println(Arrays.toString(temp));
                // p.remove(z);
                System.out.println(p);
                // System.out.println("apfkgnpa");
                line = scn.nextLine();
            } else {
                int x = Integer.parseInt(choose[0]);
                int y = Integer.parseInt(choose[1]);
                System.out.println(Arrays.toString(temp));
                gameArea.putPiece(temp[0], x, y);
                // System.out.println(gameArea);
                // gameArea.checkRows();
                line = scn.nextLine();
            }
        }
        scn.close();
    }

    */
    }
}
